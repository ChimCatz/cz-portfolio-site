Read these files first:
- index.html
- assets/css/styles.css
- assets/js/app.js
- trivia/README.md
- AGENTS.md

Goal:
Refine the Trivia block so it is simpler, shorter, and easier to maintain.

Important:
- Do NOT add Trivia to sidebar or top navbar
- Do NOT change scroll tracking
- Keep Trivia under Hero and above Professional Overview
- Do NOT modify unrelated sections

CHANGES REQUIRED:

1. Fix Trivia buttons
The Previous and Next buttons in the Trivia section must follow the same visual format as the Insights carousel buttons.

Use the existing button style as reference:
- .carousel-button
- .icon-carousel-button
- .insights-carousel-nav
- .insights-carousel-nav-next / prev

Requirements:
- buttons should be circular
- buttons should be white/light
- use the same arrow icon format as Insights buttons
- previous and next should sit side by side
- place them at the bottom-right of the Trivia block
- reuse existing classes if safe
- do not break the current Insights carousel buttons

Expected HTML style:
<button class="carousel-button icon-carousel-button hero-trivia-nav hero-trivia-nav-prev" type="button" data-trivia-prev aria-label="Previous trivia">
  <img src="assets/icons/left-arrow.svg" alt="" aria-hidden="true">
</button>

<button class="carousel-button icon-carousel-button hero-trivia-nav hero-trivia-nav-next" type="button" data-trivia-next aria-label="Next trivia">
  <img src="assets/icons/right-arrow.svg" alt="" aria-hidden="true">
</button>

2. Remove div.hero-trivia-card
Remove the extra wrapper:
- div.hero-trivia-card

The Trivia text should sit directly inside the main Trivia container/section.
Keep the layout simple with no card background, no extra border, and no heavy glow.

The section should feel like a clean continuation of the Hero section, not a separate card.

3. Reduce size
Set the Trivia block maximum height to 250px.

Requirements:
- max-height: 250px
- compact spacing
- no large padding
- text should remain readable
- avoid overflow where possible
- if needed, reduce font size or line height slightly
- desktop and mobile should both stay compact

4. Preserve content structure
Keep the Trivia display simple:
- centered H2 title: Trivia + inline icon
- trivia text inside quotation marks
- source on another line
- Previous/Next buttons bottom-right

No:
- section-kicker
- section intro
- category label
- card background
- extra decorative blocks

5. Update context files
Update:
- trivia/README.md
- AGENTS.md

Purpose:
Make future Codex sessions easier and reduce repeated context.

In trivia/README.md, add/keep:
- Trivia block lives under Hero and above Professional Overview
- It is not part of navigation
- Data is stored in trivia/trivia-data.js
- Icon is expected at trivia/trivia-icon.svg
- UI is intentionally simple: no card wrapper, no background panel
- Max height target: 250px
- Buttons should reuse the circular Insights carousel style
- App logic lives in assets/js/app.js
- Random order behavior: shuffle on load, Next cycles through shuffled list, reshuffle after end, Previous wraps backward, auto-rotate every 1 hour

In root AGENTS.md, append/update a short section:
"Trivia Block Context"
Include:
- Root folder: trivia/
- Files: trivia-data.js, trivia-icon.svg, README.md
- Position: directly under Hero, above Professional Overview
- Not a nav/scroll-tracked section
- No hero-trivia-card wrapper
- 250px max-height target
- White/simple text, source line, centered title with inline icon
- Circular white carousel buttons matching Insights style
- JS behavior in assets/js/app.js

Acceptance checks:
- Trivia no longer has div.hero-trivia-card
- Trivia section has no separate card background
- Height is capped around 250px
- Buttons are circular and visually match Insights carousel buttons
- Trivia text and source still render correctly
- Random trivia logic still works
- README.md and AGENTS.md are updated for future context
- No console errors